# TODO: Fix Resend Import in sendOTP.js

## Steps:
- [x] Step 1: Edit `src/shared/utils/sendOTP.js` - Fix import from `const Resend = require('resend');` to `const { Resend } = require('resend');` ✅
- [ ] Step 2: Verify the change
- [ ] Step 3: Mark task complete

